import NavBar from "../ui/nav-bar";

const Header = () => {
  return (
    <header className="sticky top-0 left-0 right-0 z-50 px-10 bg-blue-800">
      <NavBar />
    </header>
  );
};

export default Header;
